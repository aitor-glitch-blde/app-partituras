-- Este archivo contiene el schema completo de la base de datos
-- Para ejecutarlo, usa la consola SQL de Supabase o el script de migración
-- El contenido del schema está en el README.md principal
-- Por ahora, creamos un archivo placeholder

SELECT 'Ejecuta el schema SQL desde la consola de Supabase';
SELECT 'El schema completo está disponible en README.md';
